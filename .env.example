# Copy this file to .env.local and fill in your values.
# This file is committed to source control — never put secrets here.

# Optional. The deployed site's public URL, used for absolute metadata
# (OpenGraph/Twitter card images, canonical links). Falls back to
# http://localhost:3000 in development if unset.
NEXT_PUBLIC_SITE_URL=
